#ifndef LOG__TEST__H__
#define LOG__TEST__H__

#include <boost/thread.hpp>
#include <sys/time.h>

#include <vector>
#include <string>

//static std::vector<std::string> filenames;

//static const unsigned long COUNT = 1<<64;

typedef unsigned int uint;

class LogBench{
    private:
        uint _timespan;
        uint _filenum;
    public:
        LogBench(uint timespan,uint filenum):
            _timespan(timespan),_filenum(filenum){}

        void logStt()
        {
            while()
        }

};

#endif
